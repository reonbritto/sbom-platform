apiVersion: v1
kind: ServiceAccount
metadata:
  name: sbom-app
  annotations:
    # Workload Identity — overlay patches the client-id per env
    azure.workload.identity/client-id: "00000000-0000-0000-0000-000000000000"
  labels:
    azure.workload.identity/use: "true"
