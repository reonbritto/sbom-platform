# The sbom-app SA itself doesn't need an Azure MI annotation — it talks to
# Azure resources only via ESO (which uses its own MI in external-secrets ns).
# This patch left in place in case we add app-side direct KV access later.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: sbom-app
  annotations: {}
